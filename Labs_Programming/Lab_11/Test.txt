#include <stdio.h>

main() /* Simple program */
{
// Number.
int num;

num = l;	// Assign a variable num number "1".

printf ("I'm simple");
printf ("computer. ");
printf ("My favorite number is %d, because it's the first. ", num);
}